name: Build StudyGate APK V3 New Package

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout files
        uses: actions/checkout@v4

      - name: Rebuild Android project folders
        run: |
          rm -rf app
          mkdir -p app/src/main/java/com/example/studygate
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/res/xml

          cp AndroidManifest.xml app/src/main/AndroidManifest.xml
          cp MainActivity.kt app/src/main/java/com/example/studygate/MainActivity.kt
          cp BlockedActivity.kt app/src/main/java/com/example/studygate/BlockedActivity.kt
          cp StudyPrefs.kt app/src/main/java/com/example/studygate/StudyPrefs.kt
          cp YouTubeBlockAccessibilityService.kt app/src/main/java/com/example/studygate/YouTubeBlockAccessibilityService.kt

          cp activity_main.xml app/src/main/res/layout/activity_main.xml
          cp activity_blocked.xml app/src/main/res/layout/activity_blocked.xml
          cp strings.xml app/src/main/res/values/strings.xml
          cp styles.xml app/src/main/res/values/styles.xml
          cp accessibility_service_config.xml app/src/main/res/xml/accessibility_service_config.xml

          # 설치 충돌 방지를 위해 앱 이름만 V3로 표시합니다.
          sed -i 's/android:label="StudyGate"/android:label="StudyGate V3"/' app/src/main/AndroidManifest.xml
          sed -i 's/<string name="app_name">StudyGate<\/string>/<string name="app_name">StudyGate V3<\/string>/' app/src/main/res/values/strings.xml

          cat > settings.gradle.kts <<'EOF'
          pluginManagement {
              repositories {
                  google()
                  mavenCentral()
                  gradlePluginPortal()
              }
          }
          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
              repositories {
                  google()
                  mavenCentral()
              }
          }
          rootProject.name = "StudyGate"
          include(":app")
          EOF

          cat > build.gradle.kts <<'EOF'
          plugins {
              id("com.android.application") version "8.7.3" apply false
              id("org.jetbrains.kotlin.android") version "2.0.21" apply false
          }
          EOF

          cat > app/build.gradle.kts <<'EOF'
          plugins {
              id("com.android.application")
              id("org.jetbrains.kotlin.android")
          }

          android {
              namespace = "com.example.studygate"
              compileSdk = 35

              defaultConfig {
                  // 기존 StudyGate와 충돌하지 않도록 새 패키지 이름 사용
                  applicationId = "com.tnghco.studygatev3"
                  minSdk = 23
                  targetSdk = 35
                  versionCode = 3
                  versionName = "1.0-v3"
              }

              compileOptions {
                  sourceCompatibility = JavaVersion.VERSION_17
                  targetCompatibility = JavaVersion.VERSION_17
              }

              kotlinOptions {
                  jvmTarget = "17"
              }
          }

          dependencies {
              implementation("androidx.core:core-ktx:1.13.1")
              implementation("androidx.activity:activity-ktx:1.9.0")
              implementation("androidx.media3:media3-exoplayer:1.3.1")
              implementation("androidx.media3:media3-ui:1.3.1")
          }
          EOF

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.10.2

      - name: Build APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: StudyGate-v3-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
